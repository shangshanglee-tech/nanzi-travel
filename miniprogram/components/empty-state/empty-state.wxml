<view class="empty-state">
  <view class="empty-mark">楠</view>
  <view class="empty-title">{{title}}</view>
  <view class="empty-description">{{description}}</view>
  <button wx:if="{{showAction}}" class="empty-action" bindtap="handleAction">{{actionText}}</button>
</view>
